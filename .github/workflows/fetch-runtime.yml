name: fetch-runtime
on:
  push:
    paths:
      - "trigger-wine.txt"
permissions:
  contents: write
jobs:
  fetch:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Run bundle script from repo
        run: bash tools/fetch-runtime.sh
